This is a person named {{label.@value}}.
{{#if birthplace}}They were born in {{birthplace.@value}}.
{{/if}}
{{#if birthDate}}They were born on the date {{birthDate.@value}}.
{{/if}}
{{#if deathDate}}They died on the date {{deathDate.@value}}.
{{/if}}
{{#if names}}
  {{#each names}} {{../label.@value}} was mentioned in the following citations:
    {{#if citations}}A citation with respect to {{name.@value}}
      {{#each citations}}{{note.@value}} {{#if source}}according to the source {{source.@value}}. {{/if}}
      {{/each}}{{/if}}
    {{#if variant.length}}{{name.@value}} also has the following variant names:
      {{#each variant}}{{this.@value}}
      {{/each}}
    {{/if}}
  {{/each}}
{{/if}}